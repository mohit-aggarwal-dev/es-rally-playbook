[meta]
config.version = {{ config.version }}

[system]
env.name = {{ env.name }

[node]
root.dir = {{ root.dir }}

[runtime]
java8.home = {{ java8.home }}

[benchmarks]
local.dataset.cache = {{ local.dataset.cache }}

[reporting]
datastore.type = {{ datastore.type }}
datastore.host = {{ datastore.host }}
datastore.port = {{ datastore.port }}
datastore.secure = {{datastore.secure }}
datastore.user = {{ datastore.user }}
datastore.password = {{ datastore.password }}

[tracks]
default.url = {{ default.track.url }}

[defaults]
preserve_benchmark_candidate = {{ preserve_benchmark_candidate }}
